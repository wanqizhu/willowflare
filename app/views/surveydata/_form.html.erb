<%= simple_form_for(@surveydatum) do |f| %>
  <%= f.error_notification %>

  <div class="surveydata-form-inputs">
    <%= f.input :email %>
    <%= f.input :surveyresponse %>
    <%= f.input :reward %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
