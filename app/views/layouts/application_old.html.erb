<!DOCTYPE html>
<html>
<head>

   <meta name="description" content="WillowFlare: Mobile Gaming, Reimagined.">
    <meta name="author" content="Wanqi Zhu">
    <meta name="keywords" content="willow, flare, willowflare, mobile game, mobile gaming, feedback" />
    <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>WillowFlare</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <link href="http://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">

  <link href='http://fonts.googleapis.com/css?family=Kaushan+Script' rel='stylesheet' type='text/css'>

  <link href='http://fonts.googleapis.com/css?family=Nunito:300,400,700|Droid+Sans:400,700' rel='stylesheet' type='text/css'>
</head>
<body>

    
    
            
    <% if user_signed_in? -%>
        <nav class="navbar navbar-default">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <div id="logo" class="navbar-brand"><%= link_to "WillowFlare", root_path %></div>
                </div>

                <div class="collapse navbar-collapse navbar-ex1-collapse">
                    <ul id="topNavRight" class="nav navbar-nav navbar-right">

                        <% if current_user.auth_level >= 90 %>
                        <li><%= link_to 'Submit review', new_link_path %></li>
                        <% end %>

                        <li><%= link_to 'Earn rewards', root_path %></li>

                        <li><%= link_to 'Account', edit_user_registration_path %></li>
                        <li><%= link_to 'Sign out', destroy_user_session_path, :method => :delete %></li>
                    </ul>
                </div>

            </div>
        </nav>

    <% else -%>
       
        <div class="container" id="topBanner">
            <p>Already a member?<span><%= link_to 'Sign in', new_user_session_path %></span>
            <span><%= link_to 'Sign up', new_user_registration_path %></span></p>
        </div>
    <% end -%>

    
       

    <div id="page">
        <% flash.each do |name, msg| %>
            <% if msg != nil and !msg.empty? %>
                <%= content_tag(:div, msg, class: "alert alert-info " + name) %>
            <% end %>
        <% end %>

        <div class="container">
            <%= yield %>
        </div>

    </div>

</body>
</html>
